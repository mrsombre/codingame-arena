# TODO

1. `bin/arena <command> <game>` instead of `--game` (all commands need a game).
2. Bundle docs (about tracer, etc.) md files directly into the binary, so the agent can read them straight from the binary — no need to provide them separately.
3. Create a viewer for spring2021.
4. JSONL direct trace format for NN training.
5. Capture stderr from bots into traces.
6. Add number of matches played report every 1000 matches.
